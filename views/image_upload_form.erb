<html>
  <head>
  <title>Generador de escala de notas</title>
  <meta http-equiv="content-type" content="text/html; charset=UTF-8">
  <meta http-equiv="Content-Language" content="ES" />
  <meta name="description" content="Generador de escala de notas para profesores. Obtén tablas para diferentes niveles de exigencia, puntajes, etc." />
  <meta name="language" content="Spanish" />
  <link href="app.css" media="screen" rel="stylesheet" type="text/css">
  <link rel="icon" type="image/png" href="favicon.png">
  <script src="//code.jquery.com/jquery-1.11.1.min.js" type="text/javascript"></script>
  <script src="app.js" type="text/javascript"></script>
  <script type="text/javascript">
    // var _gaq = _gaq || [];
    // _gaq.push(['_setAccount', 'UA-11840444-3']);
    // _gaq.push(['_trackPageview']);

    // (function() {
    //   var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    //   ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    //   var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
    })();
  </script>
  </head>
  <body>
    <form action='/upload_image' method="post" enctype="multipart/form-data">
      <table>
        <tr>
          <td>Image</td>
          <td><input accept="image/png,image/gif,image/jpeg" name="image" type="file" /></td>
        </tr>
        <tr>
          <td>Filter radius</td>
          <td>
            <select name="filter_radius">
              <% @filter_radius_values.each do |val| %>
              <option value="<%= val %>"><%= val %></option>
              <% end %>
            </select>
          </td>
        </tr>
        <tr>
          <td>Threshold</td>
          <td>
            <select name="threshold">
              <% @threshold_values.each do |val| %>
              <option value="<%= val %>"><%= val %></option>
              <% end %>
            </select>
          </td>
        </tr>
        <tr>
          <td colspan="2"><input name="commit" type="submit" value="Upload image" /></td>
        </tr>
      </table>
    </form>
  </body>
</html>
